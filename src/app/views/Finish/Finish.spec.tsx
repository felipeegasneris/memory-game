import React from 'react';
import { expect } from 'chai';
import Finish from './Finish';

describe('Spec Finish', function () {
    it('it exists', () => {
        expect(Finish).to.be.ok;
    });
});
